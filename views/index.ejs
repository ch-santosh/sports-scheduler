<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sports Scheduler</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
    <style>
      /* Global Styles */
      :root {
        --primary-color: #ff6b6b;
        --secondary-color: #ffd93d;
        --accent-color: #ff8e8e;
        --text-dark: #333;
        --text-light: #666;
        --white: #ffffff;
        --transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Poppins', sans-serif;
        background-color: #fff9e6;
        line-height: 1.6;
        color: var(--text-dark);
      }

      .container {
        width: 90%;
        max-width: 1200px;
        margin: auto;
        overflow: hidden;
      }

      /* Header Styles */
      header {
        background: linear-gradient(135deg, #ff8e8e, #ff6b6b);
        color: var(--white);
        padding: 1rem 0;
        position: fixed;
        width: 100%;
        top: 0;
        z-index: 1000;
        box-shadow: 0 2px 15px rgba(255, 107, 107, 0.2);
        transition: var(--transition);
      }

      header.scrolled {
        padding: 0.5rem 0;
        background: rgba(255, 107, 107, 0.95);
        backdrop-filter: blur(10px);
      }

      header .container {
        display: flex;
        justify-content: space-between;
        align-items: center;
      }

      header #branding {
        display: flex;
        align-items: center;
      }

      header #branding h1 {
        font-size: 1.8rem;
        font-weight: 700;
        letter-spacing: 1px;
      }

      header #branding .highlight {
        color: var(--secondary-color);
        position: relative;
      }

      header nav ul {
        display: flex;
        gap: 2rem;
        list-style: none;
      }

      header nav a {
        color: var(--white);
        text-decoration: none;
        text-transform: uppercase;
        font-size: 0.9rem;
        font-weight: 500;
        letter-spacing: 1px;
        padding: 0.5rem 1rem;
        border-radius: 25px;
        transition: var(--transition);
      }

      header nav a:hover {
        background-color: rgba(255, 217, 61, 0.2);
        transform: translateY(-2px);
      }

      /* Showcase Styles */
      #showcase {
        min-height: 100vh;
        background: linear-gradient(rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.4)),
                    url("https://th.bing.com/th/id/OIP.qhcRx6ocewKyIX9v6eDkgwHaHY?rs=1&pid=ImgDetMain") no-repeat center center/cover;
        display: flex;
        align-items: center;
        justify-content: center;
        text-align: center;
        color: var(--white);
        position: relative;
        overflow: hidden;
      }

      #showcase::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(255, 107, 107, 0.3), rgba(255, 217, 61, 0.3));
      }

      #showcase .container {
        position: relative;
        z-index: 1;
        padding: 2rem;
        max-width: 800px;
        background: rgba(255, 255, 255, 0.1);
        border-radius: 20px;
        backdrop-filter: blur(5px);
      }

      #showcase h1 {
        font-size: 4rem;
        font-weight: 700;
        margin-bottom: 1.5rem;
        text-shadow: 2px 2px 8px rgba(0, 0, 0, 0.3);
        animation: fadeInUp 1s ease-out;
      }

      #showcase p {
        font-size: 1.5rem;
        margin-bottom: 2rem;
        text-shadow: 1px 1px 4px rgba(0, 0, 0, 0.3);
        animation: fadeInUp 1s ease-out 0.3s backwards;
      }

      /* Box Styles */
      #boxes {
        padding: 5rem 0;
        background-color: var(--white);
      }

      #boxes .container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        gap: 2rem;
        padding: 0 1rem;
      }

      #boxes .box {
        background: linear-gradient(145deg, #ffffff, #fff9e6);
        padding: 2.5rem;
        text-align: center;
        border-radius: 20px;
        box-shadow: 0 10px 30px rgba(255, 217, 61, 0.1);
        transition: var(--transition);
        position: relative;
        overflow: hidden;
        border: 1px solid rgba(255, 217, 61, 0.2);
      }

      #boxes .box::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, var(--secondary-color), var(--primary-color));
        opacity: 0;
        transition: var(--transition);
        z-index: 0;
      }

      #boxes .box:hover {
        transform: translateY(-10px);
        border-color: var(--secondary-color);
      }

      #boxes .box:hover::before {
        opacity: 0.05;
      }

      #boxes .box img {
        width: 100px;
        height: 100px;
        object-fit: contain;
        margin-bottom: 1.5rem;
        transition: var(--transition);
        position: relative;
        z-index: 1;
        filter: drop-shadow(0 4px 6px rgba(0, 0, 0, 0.1));
      }

      #boxes .box:hover img {
        transform: scale(1.1) rotate(5deg);
      }

      #boxes .box h3 {
        font-size: 1.5rem;
        color: var(--primary-color);
        margin-bottom: 1rem;
        position: relative;
        z-index: 1;
      }

      #boxes .box p {
        color: var(--text-light);
        font-size: 1rem;
        position: relative;
        z-index: 1;
      }

      /* Footer Styles */
      footer {
        background-color: #1a1a1a;
        color: var(--white);
        padding: 3rem 0;
        position: relative;
      }

      footer::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: linear-gradient(135deg, rgba(255, 107, 107, 0.1), rgba(255, 217, 61, 0.1));
      }

      footer .container {
        position: relative;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 1.5rem;
      }

      footer p {
        font-size: 1rem;
        opacity: 0.8;
        color: var(--secondary-color);
      }

      footer .social-links {
        display: flex;
        gap: 1.5rem;
      }

      footer a {
        color: var(--secondary-color);
        text-decoration: none;
        font-size: 1.5rem;
        opacity: 0.8;
        transition: var(--transition);
      }

      footer a:hover {
        color: var(--primary-color);
        opacity: 1;
        transform: translateY(-3px);
      }

      /* Animations */
      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      /* Media Queries */
      @media (max-width: 768px) {
        header nav ul {
          gap: 1rem;
        }

        header nav a {
          font-size: 0.8rem;
          padding: 0.4rem 0.8rem;
        }

        #showcase h1 {
          font-size: 2.5rem;
        }

        #showcase p {
          font-size: 1.2rem;
        }

        #boxes .container {
          grid-template-columns: 1fr;
        }
      }
    </style>
  </head>
  <body>
    <header>
      <div class="container">
        <div id="branding">
          <h1><span class="highlight">Sports</span> Scheduler</h1>
        </div>
        <nav>
          <ul>
            <li><a href="/">Home</a></li>
            <li><a href="/signup">Sign up</a></li>
            <li><a href="/signin">Login</a></li>
          </ul>
        </nav>
      </div>
    </header>

    <section id="showcase">
      <div class="container">
        <h1>Welcome to the Sports Scheduler!</h1>
        <p>Join and manage your sports events with ease.</p>
      </div>
    </section>

    <section id="boxes">
      <div class="container">
        <div class="box">
          <img src="https://png.pngtree.com/png-vector/20230302/ourmid/pngtree-event-management-line-icon-vector-png-image_6626145.png" alt="Event Management" />
          <h3>Event Management</h3>
          <p>Create, join, and keep track of your events effortlessly.</p>
        </div>
        <div class="box">
          <img src="https://c1.klipartz.com/pngpicture/823/765/sticker-png-login-icon-system-administrator-user-user-profile-icon-design-avatar-face-head.png" alt="User Profiles" />
          <h3>User Profiles</h3>
          <p>Customize your profile and connect with other players.</p>
        </div>
        <div class="box">
          <img src="https://i.pinimg.com/originals/50/53/e5/5053e50d44b104d89bd4d6f1358cb632.png" alt="Support" />
          <h3>24/7 Support</h3>
          <p>Our team is always here to help you with any issues.</p>
        </div>
      </div>
    </section>

    <footer>
      <div class="container">
        <p>Sports Scheduler, ©2024</p>
        <div class="social-links">
          <a href="tel:+1234567890"><i class="fas fa-phone-alt"></i></a>
          <a href="https://www.google.com" target="_blank"><i class="fab fa-google"></i></a>
        </div>
      </div>
    </footer>

    <script>
      // Add scroll effect to header
      window.addEventListener('scroll', () => {
        const header = document.querySelector('header');
        if (window.scrollY > 100) {
          header.classList.add('scrolled');
        } else {
          header.classList.remove('scrolled');
        }
      });
    </script>
  </body>
</html>